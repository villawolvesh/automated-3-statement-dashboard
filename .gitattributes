# Normalize line endings to LF in the repository so Windows and the Linux-based
# GitHub Actions runner produce identical files (no CRLF churn on auto-refresh).
* text=auto eol=lf

# Treat these as binary (never line-ending normalized)
*.db  binary
*.png binary
*.jpg binary
